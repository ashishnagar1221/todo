<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
    rel="stylesheet"
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
    crossorigin="anonymous"
  />
  <link
    rel="stylesheet"
    href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
    integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay"
    crossorigin="anonymous"
  /> 
  <link rel="stylesheet" href="/style.css">
  <title>Task Manager</title>
</head>
<body>
    <div class="container">
        <h1>Task Manager</h1>
    </div>    
    <div class="todo-part">
        <form action="/" method="POST" class="todo-head">
            <input type="text" name="task" class="add" placeholder="Add new task ..." required/>
            <button type="submit" class="main"><span class="fas fa-plus"></span></button>
        </form>
        <ul>
            <%for(let i in Todo){%>
           <li class="row">
               <%let checked = Todo[i].done ? "checked" : "" %>
                    <form class="col1" action="/check/<%=Todo[i]._id%>" method="POST">
                <input type="checkbox" class="chck" name="todo1" onChange="this.form.submit()" <%= checked %> />
                </form>
                    <span class="data col2"><%=Todo[i].task%></span>
                    <form class="col3" action="/edit/<%=Todo[i]._id%>" method="GET">
                        <button ><span class="fas fa-edit"></span></button>
                        </form>
                    <form class="col4" action="/del/<%=Todo[i]._id%>" method="GET"> 
                        <button ><span class="fas fa-trash"></span></button>
                    </form>
            </li>
            <% } %> 
        </ul>
    </div>
</body>
</html>